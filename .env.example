DATABASE_URL=postgresql://mirador:mirador@localhost:5432/mirador
WHATSAPP_NUMBER="963955000111"
# PLACEHOLDER policy (prompt-4 §9 / R0): COP-1's root — canonical/hreflang/OG/JSON-LD
# render against this origin and are never claimed deployed. Swap at deployment:
# one env var, one place. Quoting note: DATABASE_URL stays UNQUOTED — the Prisma CLI
# rejects quoted URLs (P1012) while Next's dotenv accepts both.
NEXT_PUBLIC_SITE_URL="https://mirador.example"
