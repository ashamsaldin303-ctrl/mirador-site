`````markdown
# PROMPT 6 · UI-EXCELLENCE CONTRACT — "THE SIGNATURE FLOORS" (v1.2)

**Chain:** build-brief.md v1.1 → prompt-2-evidence-run.md → prompt-3-production-readiness.md → prompt-4-release1-house-edges.md → judgment-r1.md → prompt-5-r1-closeout.md → **this contract**.
**Basis:** the human's ratification act of 2026-09-23 ("send me the prompt file for the UI components you will add, and make the site very distinctive in all aspects") = **the signature on THE SIGNATURE REGISTER** (`download/mirador/ui-excellence-analysis.md` v1.0 §4). **Roster rulings (standing since v1.1):** P-094/P-095/P-097/P-098/P-099/P-100/P-101 buildable this round · **P-096 THE TABLE CHOIR DEFERRED to the Release-2 contract** (its landing surface — the funnel's summary card — ships there; landing it on the confirmation page would implement P-039's core image, which §3 fences OUT, and collide with the canon's ONE lighting moment) · **P-102 REFUSED** (the analysis's argument stands). **Repo:** github.com/ashamsaldin303-ctrl/mirador-site @ `7849e4f` (main) at authoring time. **ACs:** E89–E99 (chain-continuous; zero collision with E82–E88). All NEVERs N1–N27 remain binding VERBATIM; **N28 and N29 are new below**. **v1.0→v1.1:** cold-start-audited (worklog Task 9-a: 2 BLOCKER · 7 MAJOR · 7 MINOR — all fixed here: the byte-economy re-priced to the measured physics with a human ceiling decision; P-096 re-seated; the dead `DialogContent` harvested; census receipts made manual-and-raw; every citation re-verified against the repo). **THE CEILING AMENDMENT — SIGNED (2026-09-23):** the human's message to the judge, verbatim: **"Option B"** — the signature on E90's CSS-ceiling decision. Effect: the CSS-additions ceiling is amended **+3KB → +5.5KB total**; **ALL SEVEN seated components land this round**; the kill order (Appendix B) stays backstop-only. Provenance: the human signature alone — N29's one lawful path, the P-092 precedent's shape — recorded here in the contract of record so the builder needs no second document and the dispatch message needs no restatement. **Distinct-decision note:** the instrument decision (PROMPT 5's R5) is a different fork on a different contract; this signature does not touch it — its standing default (Option A / lantern) governs until its own signature lands. **v1.1→v1.2:** the Option-B ceiling signature recorded (header · §1 · §2(6) · §3 · R0.5 · R4's payment note · R9 · §9(2)(3) · Appendix A(6) · Appendix B); nothing else moved — the E-numbering, every threshold, N28/N29 in force, and the measured-physics table are verbatim from v1.1.

> **الملخص العربي:** هذا عقد «الطوابق الموقّعة» (v1.2 — سُجِّل فيه توقيعك على قرار السقف). بتوقيعك صُدِّق السجلّ بمكانته الصادقة: **المكوّنات السبعة كلّها تُبنى هذه الجولة** (نظام النافذة P-094، طبق النافذة P-100، قائمة الطوابق P-095، سطح المرصد P-099، ساعة المدينة P-097، ضوء الغرفة P-098، الطابق غير المبنيّ P-101)، و**كورال الطاولات P-096 يؤجَّل لعقد الإصدار الثاني** — لأنّ سطح هبوطه (بطاقة الملخّص في القمع) يُبنى هناك، وإنزاله على صفحة التأكيد يسرق لحظة الإشعال المحميّة · ولوحة المغادرة P-102 تبقى مرفوضة. **قرار السقف وُقِّع:** اخترتَ **(ب)** — رفع سقف CSS من +3KB إلى **+5.5KB** (توقيع الإنسان، 2026-09-23، حرفيًا: «Option B»، مُسجَّل في ترويسة العقد وفي نموذج المستودع E90). الفيزياء لم تتغيّر: الحذف الواقعي المتاح ≈0.3KB فقط، والسجلّ كان **مستحيلًا فيزيائيًا** تحت السقف القائم — لكن قرارك وسّع السقف، فتهبط السبعة كلّها، ويبقى ترتيب الإعدام ضامنًا إن تجاوزت القياساتُ الفعلية. لا وكيل يلمس سقفًا مجمّدًا بعدها (N29). عتبات Lighthouse لا تُمسّ، وحالة E79 تُنقل من الأدلّة الخامّ بلا تجميل. **الترتيب القائم: البرومبت الخامس أوّلًا** إلا بأمرك الصريح المُسجَّل. وعند خضوع البطارية: **توقّف**.

## §1 · MISSION & ROLE

You are the same builder agent, same discipline (self-verifying reports, FAIL honesty, no partial credit, ambiguity = defect → report, never guess). The judge audited the live tree (43 components / 8 families / 3,627 lines @ `7849e4f`) and code-pinned five findings (ui-excellence-analysis.md §3): F1 exceptional grammar, austere surface · F2 the canon is on paper · F3 three overlay entrance geometries, nothing says "same building" · F4 the city does not know the hour · F5 the site's data never becomes light. **The human's ratification act AMENDS the sealed train order:** the register's Release-3-seat items land NOW, absorbing their minimal infrastructure (the horizon datum, the one-door verb, the stagger utilities); the Release-2/3/4 contracts re-cite what has landed instead of re-speccing it. Your job is to land the register EXACTLY as seated — not improved, not extended, not invented past (N28) — under the ceiling amendment the human has ALREADY signed (the CSS ceiling → +5.5KB, recorded in the header; N29's one lawful path, now exercised — you never touch it again).

## §2 · ACK-FIRST PROTOCOL — no code before the ACK

Before your first commit, post the ACK form (Appendix A) quoting verbatim: (1) the map and your stop + the STOP rule; (2) the ceilings table (§5) copied whole, the N29 clause included; (3) the ΣΔ≤0 law + Appendix B's measured-physics table copied whole; (4) your SITE_URL value or the PLACEHOLDER policy; (5) **the close-out standing order**: PROMPT 5's DONE on record BEFORE this contract's first product commit — OR the human's dispatch message quoted verbatim if it explicitly reorders (the reorder is the human's amendment, logged; your DONE then discloses the FULL pre-repair record — the J-1..J-6 register, not J-3 alone — and pins E79's carry to the RAW medians verdict, never to a doc row); if neither is on record, STOP at ACK and report; (6) **the ceiling decision's standing state: SIGNED — Option B** (the human's verbatim message "Option B", 2026-09-23, quoted in this contract's header): the CSS ceiling is **+5.5KB total** from this round's start · all seven seated components land · the kill order (Appendix B) is the backstop if measured cells overshoot · you never touch, re-baseline, or re-open the ceiling (N29), and the form (E90) RECORDS the signed amendment — it does not solicit one; (7) the ratification roster with its rulings (7 buildable · P-096 deferred to Release 2 · P-102 refused · the ten-pattern kill list stays NEVER); (8) every contradiction you find in this contract. A false or partial ACK voids the DONE (rule-zero inheritance).

## §3 · SCOPE

**IN:** R0.5's decision-form RECORD (the signed amendment) · R1 the horizon datum (`--horizon: 18%` — P-038's minimal essence, re-seated here) · R2 the window system `<AtTheWindow>` (P-094 — P-048 One-Door's frame composed; the descent verb = the existing `settle` verb) · R3 the plate at the window (P-100) · R4 the floor-plate menu (P-095 — re-seated from the P-013/P-040 train; numerals beside EXISTING titles, zero new copy) · R5 the observation deck (P-099 — the gallery application of the stagger utilities, re-seated from P-014; **altitude is DERIVED at render from index-within-collection — NO schema change, no migration, no new field; the analysis's "schema-additive key" is overruled in the safer direction, and this ruling is part of the contract**) · R6 the city's clock (P-097 — `data-night` buckets, color-only) · R7 the room's light (P-098 — a STATIC three-level luminance channel; the analysis's breathe-rider is amended out — the stillness census (Release 3's "0 at-rest movers on the six task routes") must not be pre-broken; the room's breath becomes the room's light thinning, stated in the twins doc) · R8 the unbuilt floor (P-101) · R9 the exit battery + DONE + doc registrations (`component-inventory` · `idiom-contracts` + the sheet's slide exception · `figure-register` · `twins` · `copy-parity` · `exchange-ledger` + the new CSS-cell instrument · MATRIX + POINTERs). **Code surfaces permitted:** `src/components/**` · `src/app/globals.css` (within the ceiling law in force) · the route files the components compose into · **`content/en.json` + `content/ar.json`** (the round's authored copy lands there — the copy-parity law rides) · `evidence/tools/**` + `scripts/**` (R9's census receipts + the CSS-cell instrument). Nothing else.

**OUT (N26 inheritance + this round's fence):** every Release-2/3/4 roster item NOT named above — **P-046/P-056 funnel instruments stay Release 2's (P-098 composes with whatever lands later; the Release-2 contract re-cites it)** · **P-096's build (deferred — its seat is Release 2's funnel cluster; building it here anyway = a defect returned for revert)** · P-034 the rail · P-039 the city echo · the ceremony cluster · P-076 dark floors · P-102 (REFUSED) · any invention beyond the roster (**N28**) · any threshold/instrument change (**N27**) · any ceiling change by an agent (**N29**) · any new font face (the floor-plate numerals and the dial ride the DEFERRED full display families — `fonts-deferred.css`, below-fold, the run-23 architecture; zero additions to font disk or the preloaded set).

## §4 · TASK DAG (R0 → R9)

**R0 · ACK (E89)** — the form posted before the first commit, eight items verbatim (§2).

**R0.5 · THE CEILING DECISION FORM (E90) — the RECORD of a decision already signed by the human, authored by you, never signed by you.** The signature landed PRE-DISPATCH: **Option B** (the human's verbatim message "Option B", 2026-09-23 — quoted in this contract's header): the CSS ceiling is amended **+3KB → +5.5KB total**; ALL seven seated components land; the kill order (Appendix B) stays backstop-only. Author `docs/css-ceiling-decision.md` BEFORE the first product commit as the decision's in-repo record: **Option A — the +3KB ceiling stands** (NOT taken: the landing set would have thinned to what the measured physics affords — P-094 first, then P-101 and P-098 only if the measured deletion surplus paid them; P-095/P-097/P-099/P-100 deferred with named seats — the R3-A trains re-cite them) · **Option B — the human amends the CSS ceiling to +5.5KB total** (TAKEN, signed: the register's full gross ≈+2.55KB + the program's 0.03KB slack + margin; ALL seven components land, the kill order as backstop). Both options state the arithmetic from Appendix B's MEASURED column, the trade in product terms (A = maximal discipline, a thinner signature round; B = the full ratified register, a one-time +2.5KB CSS cost), and the precedent (P-092's soft-cap amendment — the same signature shape). The signature field carries ONLY the provenance, filled verbatim as: `signed by the human, 2026-09-23 — verbatim "Option B", via PROMPT 6 v1.2's header; recorded by the builder, never signed by the builder` + the record's date. **Do NOT sign it yourself. Do NOT re-open, re-weigh, or re-litigate the decision — the signature governs; the round starts under B.** **E90 =** the form committed before the first product commit · both options' arithmetic traceable to Appendix B · the signature field carries the provenance above verbatim · the ACK states the governed option (B, signed pre-dispatch).

**R1 · THE HORIZON DATUM (E91).** `--horizon: 18%` in `globals.css` as the single source; the journey's hardcoded `bottom-[18%]` hairline (journey.tsx:172 — the sole literal in src) migrates onto it (kill-or-replace: a magic number dies); every consumer below renders the datum from the property. **E91 =** the property defined · grep: zero remaining `bottom-\[18` literals in src · journey visual-parity screenshot (375px ×2 locales, unchanged) · CSS delta ≤ +0.05KB.

**R2 · THE WINDOW SYSTEM (E92).** One layout primitive (`src/components/ui/at-the-window.tsx` — Radix Dialog underneath, a11y contract byte-identical: focus trap, Esc, `aria-modal`, described content). The surface: the graded night scrim + the horizon hairline at the datum + one close affordance at `end-4` + the content slot. **Entrance = the existing `settle` verb (600ms vertical arrival; RM twin = the standing fade ≤300ms grammar). Exit = fade at `duration-fast` (100ms) — the existing closed-state tempo; no new verb, no new keyframe.** **Migration (the three LIVE surfaces):** `dish-overlay` (its zoom dies — dish-overlay.tsx:39) · `lightbox` (its bespoke fade classes die — lightbox.tsx:76) · `sheet` (KEEPS its mobile slide ergonomics as the REGISTERED exception in `idiom-contracts.md`, but inherits the scrim grading + the close law). **The dead `DialogContent` in `ui/dialog.tsx` (zero importers — verified) is DELETED entirely; its utilities are harvested as payment** (Appendix B). **E92 =** the three live surfaces render through the primitive (grep: ad-hoc `data-[state=open]:animate-*` entrance families exist ONLY inside the primitive + the sheet's registered exception · grep: `DialogContent` gone from src) · keyboard walkthrough evidence (open/navigate/close per surface) · before/after screenshots ×3 surfaces ×2 locales ×375px · axe 0/0 per migrated surface · the CSS + JS cells of the round measured (R9's instrument) with P-094's net stated honestly.

**R3 · THE PLATE AT THE WINDOW (E93).** The dish overlay composes ON the window: the dish image staged on the lower third (a table by the window), the horizon hairline behind it, the allergen/diet facts as the quiet view notes. Pure composition — `LadderImage`, the existing allergen table (role="table"). **E93 =** staging screenshots ×2 locales ×375px + 1440px · semantics unchanged (axe on the open overlay) · no new JS logic or deps; the byte delta itemized in the ledger (markup deltas are bytes — state them, never claim a hard zero).

**R4 · THE FLOOR-PLATE MENU (E94).** The menu's six section headers become floor plates: the floor numeral (Western digits — the locked numeral policy; `aria-hidden` decoration) in the deferred display face BESIDE the existing section title, a hairline at the plate's base, the `draw` verb (200ms) on plate reveal, the section-nav re-keyed to floor numerals. SSR-only. **E94 =** menu screenshots ×2 locales ×3 widths (375/768/1440) · heading hierarchy unchanged (axe clean; the numeral is decoration, the H2 remains the title) · zero client JS added · zero new font bytes (grep: no new face declarations) · the CSS cost carried in Appendix B's ledger honestly (the current heading treatment is shared utilities with no deletable bespoke CSS — the payment is the signed ceiling amendment's (Option B, 2026-09-23), stated as such).

**R5 · THE OBSERVATION DECK (E95).** The gallery hangs at staggered altitudes: column offsets derived at render from index-within-collection (deterministic, SSR-stable, **no schema change — the §3 ruling**); each frame a window; captions gain the authored bilingual sighting locution (~16 lines, same author both scripts, **floors pinned to the six-floor spine — Floor 1–6 only**, registered in `copy-parity.md`, landed in `content/{en,ar}.json`); the lightbox enters via R2's window. **E95 =** gallery screenshots ×2 locales ×375px + 1440px · DOM order = visual order (CSS offsets only, zero `order` tricks — the tab walk evidenced) · the sighting copy registered · the stagger utilities itemized in the ledger.

**R6 · THE CITY'S CLOCK (E96).** `data-night` on `<html>` set once at hydration from `Date`: **dusk 18:00–20:00 · full night 20:00–23:00 · late 23:00–18:00 (all remaining hours)** — no unpinned "close". Color-only custom-property swaps (scrim depth, grain opacity, the surface lift) + the skyline canvas's resting-density constant per bucket. Zero cookie/storage/network (the zero-RUM law untouched — the hour never leaves the device). SSR renders full-night; the hydration bucket shift is color-only, ≤200ms crossfade, DISCLOSED in `twins.md`. **`?night=dusk|full|late` = the hour-bucket override — an axis DISTINCT from P-067's `?d=` (the kept-night DATE, Release 2's roster item): hour-of-day vs date, different params, different semantics; both axes registered in `twins.md` so Release 2's landing composes, not collides.** The addressable-state precedents in the repo today: `?webgl=off` (journey.tsx:73) and `?diet=` (menu-client.tsx:101). **E96 =** three buckets screenshotted via the param ×2 locales · contrast re-verified per bucket (every token pair keeps its AAA/AA class — receipt filed) · grep: zero storage/network calls added · RM zero delta · skyline density constant wired (one constant per bucket).

**R7 · THE ROOM'S LIGHT (E97).** The TimeGrid gains the pre-attentive STATIC channel: `remaining` 4+ = the steady hairline · 2–3 = the hairline at 70% · **1 = the hairline at full amber + the count numeral in amber** (the strongest static signal — NO loop, NO breathe rider; the stillness census is sacred). Three utility classes keyed off `remaining` (already in the slot DTO — zero API change). The text count stays (redundant-encoding law). **E97 =** screenshots at the three scarcity levels ×2 locales · the aria-labels UNCHANGED verbatim (time-grid.tsx:84–86's format — grep) · the classes direction-agnostic · zero new at-rest motion anywhere on /reserve (grep receipt: no animation added to time-grid).

**R8 · THE UNBUILT FLOOR (E98).** The 404 gains its punchline: the floor numeral stuck between plates — two stacked numerals in the deferred display face, `overflow` clip, a STATIC half-offset (a stuck dial does not move — that is the joke; zero motion). `aria-hidden` stack; the H1 copy unchanged. **E98 =** screenshots ×2 locales · zero motion (grep: no animation on the dial) · ≤ +0.1KB CSS.

**R9 · THE EXIT BATTERY + THE DONE REPORT (E99).** **First: the CSS-cell instrument lands** — the compiled CSS measured on the Actions build (the emitted route CSS, gz), baseline = the same measurement on the pre-round HEAD **recorded as a raw receipt BEFORE the first product commit**, Δ filed; `scripts/exchange-ledger.ts` gains the CSS cell's measurement (today it is hardcoded "—" — the judge's audit finding, fixed here). **Then one Actions dispatch:** Lighthouse ×3 ×2 locales, medians regenerated from THIS run, on the plain doors (the buckets are color-only and metrics-invariant — the reasoning stated in the DONE; no URL drift near N27) · the instrument runs whatever state is on record at dispatch (standing: lantern-simulate; if the human's instrument-decision signature from PROMPT 5's R5 — a decision DISTINCT from this contract's ceiling amendment — exists, the amended record governs and BOTH medians blocks re-emit per PROMPT 5's R4 durability law) · 16/16 first-load budgets · axe · 404 · race · spinner · the six organs · audit:motion/idioms/twins/fonts/tokens · **the census receipts MANUAL and RAW** (audit:motion implements M-2/M-3/M-4/M-6 only — M-1/M-5 do not exist yet; so: the authored-keyframes census documented with a grep receipt — 3 today, cap 7, zero new this round; the loop-family census documented the same way — 2 today, zero new) · the Exchange Ledger regenerated with this round's measured cells (Appendix B) · MATRIX + POINTERs regenerated (this round's packs under `evidence/ui/`; battery raws under `evidence/prod-run/*`) · the proof sheet gains the new states (window open/closed ×3 surfaces · night buckets ×3 · scarcity ×3 · the dial) — every capture pins `?night=` for determinism except the Lighthouse runs above. **E99 =** the raw artifacts land per the convention · the DONE at `evidence/ui/DONE.md`: the per-AC table (E89–E99) + **E79's disposition carried EXACTLY as the RAW medians verdict states it on record** (EN 3340 · AR 3904 · AR score 87 — the raws govern, never a doc row; if the close-out (PROMPT 5) has landed and closed/carried/re-proven it, THAT record is quoted with its run-ids) + the ceiling decision's governed option (B — signed 2026-09-23) + landed-vs-deferred roster (honest, per the measured cells; if the kill order fired under B, its casualties are documented as killed) + reproduction commands + LANDED addendum.

## §5 · CEILINGS (frozen — copy verbatim into the ACK; this round may not touch one cell)

| Budget | Ceiling | Note |
|---|---|---|
| First-load JS hard | ≤200KB gz, 16/16 | unchanged |
| First-load JS soft | 165KB standing default (a) | P-092; gates Release 2 |
| Motion family (lazy) | ≤62KB gz | 58.85KB pinned |
| Three-pack (lazy) | ≤235KB gz | 231.9KB, headroom 3.1KB |
| Combined rental | ≤297KB | 291.15KB |
| CSS additions | **+5.5KB total — human-signed amendment 2026-09-23 (Option B; was +3KB)** | standing subscription ≈2.97KB + register gross ≈+2.55KB ≈ 5.52KB modeled — the kill order is the backstop on measured overshoot; **N29 unchanged in force: the human signature was and remains the only amendment path, logged in `docs/css-ceiling-decision.md`; agents never touch this cell** |
| Keyframes | authored census **3 today · cap 7 — zero new this round** | MANUAL grep receipt (M-5 does not exist yet) |
| Loop families | **2 — zero new this round** | MANUAL census receipt (M-1 does not exist yet); P-098 is static by amendment |
| Fonts | AR faces ≤60KB each · disk 334,880B baseline | zero new faces (numerals/dial ride fonts-deferred) |
| Pointer listeners | **0** | D-25 — the register adds none |
| Net bytes | **ΣΔ≤0 per release** | the 7-cell Exchange Ledger + the new CSS-cell instrument (R9) |
| Lighthouse gates | ≥90 / LCP ≤2.5s / CLS ≤0.1 / TBT ≤300ms (mobile, 3 runs, median) | **FROZEN — N27; E79's disposition carried per the raw medians record** |
| Visitor measurement | zero RUM/beacons/SDK | the clock's hour never leaves the device |

## §6 · EVIDENCE CONTRACT (inherited + this round)

All prior law rides (raw artifacts only, summaries beside never instead · surface labels · Actions = the only sanctioned prod surface · §6.1-verbatim postgresql — untouched, no migration this round · MANIFEST + REPLAY per pack · PLACEHOLDER labeling · screenshots `route--state--width.png`). **Additions:** every new state named in R9 has its capture in the proof sheet · the `?night=` param + the `?d=` axis distinction documented in `twins.md` + the POINTERS · the before/after migration pairs (R2's three surfaces) committed beside the afters · the window primitive + the sheet's slide exception registered in `idiom-contracts.md` · the pre-round CSS baseline receipt (R9's instrument, taken before the first product commit) · the ledger's round table (Appendix B) reproduced in the DONE with MEASURED cells — the analysis's projections are history, the measurements are the law · the census receipts raw (grep output committed, not summarized).

## §7 · NEVERs (N1–N26 verbatim by reference; N27 restated; N28–N29 new)

- **N27 — thresholds and instruments are frozen to agents** (verbatim from PROMPT 5).
- **N28 — the register is closed.** The buildable surface is EXACTLY the seven seated components (P-094/P-095/P-097/P-098/P-099/P-100/P-101) as amended in this contract; P-096 is Release 2's; P-102 is refused; the analysis's ten-pattern kill list (§5 there: custom cursor, sound, magnetic buttons, 3D tilt, preloaders, confetti, creative skeletons, scroll-jacking, chat widgets, marquees) stays NEVER. An improvisation is not initiative — it is a defect, returned for revert.
- **N29 — ceilings are frozen to agents.** No agent may raise, re-baseline, or route around a GATEBOOK ceiling — least of all by treating projected deletions as measured. The CSS ceiling's amendment belongs to the human signature alone, logged in the decision record (the P-092 soft-cap precedent's shape). A round that cannot pay within the standing ceiling thins its roster and says so — it never borrows from a frozen cell. (The 2026-09-23 Option-B signature is the one lawful exercise of that amendment path — recorded in the contract of record; it changed the CSS cell's ceiling, not this law.)

## §8 · DONE DEFINITION (the STOP rule, scoped)

DONE = E89–E99 all PASS with regenerated raw evidence + the exit dispatch green on every component the raw files can honestly green (a FROZEN-threshold FAIL is reported as FAIL with diagnosis, never polished — N16/N21/N23/N24) + the ceiling decision's governed option on record (B — signed 2026-09-23) + the Exchange Ledger's round cells within the law in force (**under the signed B: cumulative CSS ≤ 5.5KB, with the kill order's casualties documented if it fired**) + E79's disposition carried from the raws exactly + the ACK on record. **When the seated roster is landed (or lawfully deferred) and the gates are green — STOP.** The Release-2 contract (THE HONEST DOOR) is authored on this round's APPROVE — it re-cites P-096 and P-098's composition with P-046/P-056. Gold-plating beyond the roster is a defect, returned for revert.

## §9 · HUMAN INPUTS (defaults in force)

1. **The dispatch order** — standing: PROMPT 5's DONE first, then this contract; the human's explicit reorder is an amendment, logged and disclosed per §2(5). Silence = the standing order.
2. **The ceiling decision** (E90's form) — **SIGNED: Option B** (the human's verbatim message "Option B", 2026-09-23, quoted in this contract's header): the round starts and ends under the +5.5KB ceiling; no further input is needed on this decision. The judge's recommendation on record (B — consistent with the human's stated distinctiveness priority) is moot beside the signature itself.
3. **The instrument decision** — whatever state PROMPT 5's R5 form is in at dispatch time (standing default: Option A / lantern). **A different decision on a different contract: the ceiling signature above does not touch it.**
4. **SITE_URL** — PLACEHOLDER policy `https://mirador.example` unchanged.

## APPENDIX A · ACK TEMPLATE

```
ACK — PROMPT 6 · UI EXCELLENCE · THE SIGNATURE FLOORS (v1.2)
1. Map + my stop: the seated register now (7 buildable · P-096 → Release 2 · P-102 refused); Release-2/3/4 resume after this DONE; STOP rule accepted (…quote §8 one line…).
2. Ceilings: (…copy §5 table verbatim, the N29 CSS row included…)
3. ΣΔ≤0 law + Appendix B's measured-physics table: (…copy whole…)
4. SITE_URL: <value> | PLACEHOLDER policy accepted.
5. Close-out standing order: PROMPT 5 DONE on record | OR the human's reorder quoted verbatim + the full J-1..J-6 disclosure accepted | OR STOPPED-at-ACK (delete what does not apply).
6. Ceiling decision: **Option B — SIGNED by the human, 2026-09-23** (verbatim "Option B", quoted in this contract's header): CSS ceiling +5.5KB total; all seven land; the kill order is the backstop; N29 accepted — I never touch a ceiling cell.
7. Ratification roster accepted: P-094/095/097/098/099/100/101 build · P-096 DEFERRED to Release 2 · P-102 REFUSED · the kill list stays NEVER (N28) · ceilings frozen to agents (N29).
8. Contradictions found: <list or NONE>.
```

## APPENDIX B · THE MEASURED PHYSICS + THE PAYMENTS (copy whole into the ACK)

**The deletion inventory (measured against the repo by the judge's audit — the builder re-measures at R9's baseline):** the zoom utilities everywhere (≈143B compiled) · the lightbox's bespoke fades (≈145B plausible — the window's scrim re-adds an equivalent fade, so ≈0 net there) · **the dead `DialogContent` + its centering utilities (≈250–400B — the only directed dead-code harvest)** · the TSX class-string deletions land in the JS cells (~0.25KB gz across dish/lightbox/dialog), NOT the CSS cell. **Total certain-to-plausible CSS deletion: ≈0.15–0.7KB. The sheet's slide is a REGISTERED EXCEPTION — it is not a payment and may not be counted as one.**

| Line | Gross CSS (analysis's projection) | Payment | Truth |
|---|---|---|---|
| P-094 the window (primitive ≈+0.3KB) | +0.3KB | the deletion inventory above | net ≈0 ± the measured surplus |
| P-095 the plates | +0.4KB | **none exists** (shared utilities today — nothing bespoke to delete) | paid by the signed amendment (Option B, 2026-09-23) |
| P-097 the clock | +0.8KB | none | paid by the signed amendment (Option B, 2026-09-23) |
| P-098 the room's light | +0.15KB (static — cheaper than the analysis's rider) | the measured surplus, if any | paid by the signed amendment (Option B, 2026-09-23) |
| P-099 the deck | +0.5KB | none | paid by the signed amendment (Option B, 2026-09-23) |
| P-100 the plate | +0.3KB | none | paid by the signed amendment (Option B, 2026-09-23) |
| P-101 the dial | +0.1KB | the measured surplus, if any | paid by the signed amendment (Option B, 2026-09-23) |
| **Round** | **≈+2.55KB gross vs ≈0.15–0.7KB deletable** | **the standing ceiling afforded ≈0.03KB slack** | **the full register was impossible under Option A — this is the physics; the ceiling decision existed because of it, and the human signed B (2026-09-23): ceiling +5.5KB, all seven land, kill order backstop-only** |

**Option A (ceiling stands — NOT TAKEN; recorded for the form's arithmetic):** the landing set would have thinned to P-094, then P-101 and P-098 only if the measured surplus paid them; P-095/P-097/P-099/P-100 would DEFER (named seats: the R3-A trains — P-038/P-048/P-014's own diffs re-cite them). **Option B — GOVERNING, SIGNED 2026-09-23 (ceiling → +5.5KB by the human's signature):** all seven land; **the kill order (backstop only):** P-101 → P-099's stagger tail (the deck degrades to the even grid) → P-100 (the plate re-centers) → P-097 reduced to two buckets (dusk/full — late dies) → **STOP. P-094, P-095, P-098 never slide.** Fonts: zero new faces. First-load JS: zero additions by construction (the R2 migration's TSX deletions pay the JS cells' way).

## APPENDIX C · KEY ANCHORS 

Palette night `#0A0A0B` · amber `#CBA35C` · BEZEL rgb(203,163,92) 2px/2px · seed **9621** · the journey's `bottom-[18%]` hairline = journey.tsx:172 (the sole literal) · the three entrance geometries: sheet slide (sheet.tsx:63) · the LIVE zoom (dish-overlay.tsx:39 — dialog.tsx:63's copy is DEAD code, zero importers) · the lightbox fade (lightbox.tsx:76) · the three authored keyframes settle/draw/breathe (globals.css:462–473) · durations 100/200/350/600ms · the aria format (time-grid.tsx:84–86) · `VENUE.tablesPerSlot` = 12 (src/lib/venue.ts:17 — NOT "the schema's number") · the addressable-state precedents `?webgl=off` (journey.tsx:73) + `?diet=` (menu-client.tsx:101) · the confirmation summary is `reserve/summary.tsx` = ReservationSummary (consumed ONLY by confirmation/[id]/page.tsx — the funnel has NO summary card: P-096's deferral ground) · E79 standing: EN LCP 3340 · AR 3904 · AR score 87 (the raw medians govern).
`````