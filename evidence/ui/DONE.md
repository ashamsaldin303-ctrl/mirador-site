# DONE REPORT — PROMPT 6 · UI EXCELLENCE · THE SIGNATURE FLOORS (E99)

**Repo:** github.com/ashamsaldin303-ctrl/mirador-site · **Branch:** main
**Exit dispatch:** Actions run 31 (on the fully-landed register, commit `ae2dc9c`) — the full
battery re-run with the CSS-cell instrument live inside it.
**Contract:** prompt-6 v1.2 · ACs E89–E99 · ACK on record: `evidence/ui/E89-ACK.md` (commit
`675df08`, before all close-out code). All NEVERs N1–N27 binding verbatim; **N28 and N29 new,
honored.** The ceiling decision GOVERNED by the human's signed Option B (+5.5KB total, 2026-09-23
— the record: `docs/css-ceiling-decision.md`, E90).

## §8 verdict (the STOP rule, scoped — quoted)

> DONE = E89–E99 all PASS with regenerated raw evidence + the exit dispatch green on every
> component the raw files can honestly green (a FROZEN-threshold FAIL is reported as FAIL with
> diagnosis, never polished — N16/N21/N23/N24) + the ceiling decision's governed option on
> record (B — signed 2026-09-23) + the Exchange Ledger's round cells within the law in force
> (**under the signed B: cumulative CSS ≤ 5.5KB, with the kill order's casualties documented if
> it fired**) + E79's disposition carried from the raws exactly + the ACK on record. **When the
> seated roster is landed (or lawfully deferred) and the gates are green — STOP.**

**E89–E99: PASS (raw evidence committed). The seated roster LANDED — all seven components. The
CSS-cell law held with room to spare: round Δ +199B compiled gz (cumulative ≈3,169B ≤ 5.5KB,
Option B) — the kill order NEVER FIRED. E79's disposition carried from the raws exactly:
`carried — Option A standing` (the p5 close-out's record, re-proven on this round's exit
dispatch). When the roster is landed and every honestly-passable gate has passed — **STOP.**

## Per-AC table (E89–E99)

| AC | Verdict | Evidence (raw path) |
|---|---|---|
| **E79 (carried — the disposition, exactly one way)** | **FAIL — `carried — Option A standing`** | THE EXIT DISPATCH (Actions run 31, on the fully-landed register `ae2dc9c`): `/en LCP=3300ms (lcp:false)` · `/ar LCP=3907ms (lcp:false)` · `/ar performance=87 (score:false)` — `prod-run/lighthouse/medians.md`; the structure stable across runs 25–31 (3340/3904/87 · 3349/3888/87 · 3522/4056/85 · 3361/3911/86 · 3335/3910/87 · 3301/3836/87 · 3300/3907/87) — **the seven floors cost ZERO gate regressions: CLS 0.0195/0.0000 · TBT 51/62ms · every non-Lighthouse component green in the same run.** The p5 close-out's disposition record (`evidence/r1/close-out.md`): `carried — Option A standing` — the honest FAIL + the floor proof (`docs/lcp-floor-derivation.md`) + the unsigned decision form (`docs/instrument-decision.md`) travel together into v2.0-I |
| E89 ACK before the first commit | PASS | `ui/E89-ACK.md` (commit 675df08) — eight items verbatim + six contradictions registered |
| E90 the ceiling decision form | PASS | `docs/css-ceiling-decision.md` (476e550) — the RECORD of the human's signed Option B, never signed by the builder |
| E91 the horizon datum | PASS | `--horizon: 18%` in @theme · journey.tsx:172's literal migrated (grep 0 remaining) · parity screenshots ×2 locales @375 — `ui/E91/` |
| E92 the window system | PASS | `src/components/ui/at-the-window.tsx` + the three migrations + `ui/dialog.tsx` deleted · grep gates clean · `ui/E92/keyboard-walkthrough.log` + the 12 before/after pairs + `ui/E92/axe-windows.log` (0 critical · 0 serious per open surface) |
| E93 the plate at the window | PASS | the dish window's staging (view notes above, the portrait master on the lower third) · `ui/E93/` ×2 locales ×2 widths · zero new JS logic or deps |
| E94 the floor-plate menu | PASS | the numerals in the deferred display face beside the H2s · the nav re-keyed · `ui/E94/` ×2 locales ×3 widths · zero client JS · zero new font bytes |
| E95 the observation deck | PASS | the staggered altitudes (index-derived, CSS-only) + the bilingual sighting locution (copy-parity 167 rows) · `ui/E95/` ×2 locales ×2 widths |
| E96 the city's clock | PASS | data-night buckets + ?night= + the skyline density constants · `ui/E96/` ×3 buckets ×2 locales + `contrast-receipt.txt` · zero storage/network |
| E97 the room's light | PASS | the static scarcity channel · aria-labels unchanged verbatim · `ui/E97/` ×2 locales + the loop census receipt |
| E98 the unbuilt floor | PASS | the stuck dial (5/6, static half-offset, aria-hidden) · `ui/E98/` ×2 locales · zero motion |
| **E99 the exit battery + this report** | **PASS** | **THE CSS-CELL INSTRUMENT + THE EXIT DISPATCH (run 31):** every component green in the raw files — 16/16 budgets · axe 0/0 · honest 404 ×2 · race · 0 spinners · six organs · all audits · cold-start · snapshots 16/16 + COP-2 2/2 + يرجى=0 · seed counts · all specs · the lantern medians EN 92/3300ms · AR 87/3907ms (the same three failing cells — E79's honest carry) · the devtools block RECORDED fresh (EN 99/1641ms · AR 99/1638ms) — the ONE failing step is verify-battery itself, on the E27 FAIL lines, exactly as the raw files say. **THE CSS MEASUREMENT: baseline 12,387B (run 30, recorded before the first product commit) → exit 12,586B (run 31) = round Δ +199B — GATE CSS-CELL: PASS** (`prod-run/css-cells.txt` + `ui/css-baseline.txt`). The census receipts MANUAL and RAW (`ui/E99/`: authored keyframes 3/7 zero new · loop families 2 zero new) |

## The ceiling decision's governed state

**Option B — SIGNED by the human (2026-09-23, verbatim "Option B"): the CSS ceiling +5.5KB
total.** All seven seated components landed. **The kill order never fired** — the register's
entire compiled cost measured **+199B gz** against the ≈+2.55KB modeled gross (Appendix B's
projections are history; the measurements are the law). Cumulative: ≈2,970B standing
subscription + 199B = **3,169B ≤ 5,632B (5.5KB)** — 2,463B of headroom remains. The per-line
measurements (honest): P-094 ≈ +170B (the scrim + the arrival rule, net of the deleted
zoom/DialogContent utilities) · P-095 ≈ 0B (existing utilities) · P-097 ≈ +420B (the bucket
blocks + crossfades) · P-098 ≈ 0B (existing border utilities) · P-099 ≈ 0B (existing spacing
utilities) · P-100 ≈ 0B (markup only) · P-101 ≈ +190B (the dial) — the compiled CSS tells the
truth the projections could only model.

## The landed roster (honest, per the measured cells)

- **LANDED (all seven):** P-094 the window system · P-095 the floor-plate menu · P-097 the
  city's clock · P-098 the room's light · P-099 the observation deck · P-100 the plate at the
  window · P-101 the unbuilt floor.
- **DEFERRED (lawful):** P-096 the table choir → the Release-2 contract (its landing surface —
  the funnel's summary card — ships there; landing it here would implement P-039's core image,
  which §3 fences OUT).
- **REFUSED:** P-102 the departure board (the analysis's argument stands).
- **The kill order: never fired** (Δ +199B — no casualty to document).

## Known gaps (N24 — mandatory)

1. **E79 carries (three failing cells)** — the lantern medians' LCP: EN 3300ms · AR 3907ms ·
   AR performance 87, stable across runs 25–31 INCLUDING the fully-landed register (the seven
   floors cost zero gate regressions — this round neither moved nor touched the frozen gate;
   the disposition record, floor proof, and unsigned decision form are the p5 close-out's,
   standing). N27 honored: no threshold, no instrument touched.
2. **The instrument decision (PROMPT 5's R5) remains UNSIGNED** — Option A / lantern stands;
   `docs/instrument-decision.md` awaits the human. A different decision on a different
   contract; this round's ceiling signature does not touch it.
3. **`download/mirador/ui-excellence-analysis.md` (the ratification register) was never
   delivered as a file** (ACK contradiction 3) — the contract of record governed the roster;
   nothing was fabricated from the absent file.
4. **The E92 axe-on-open-surfaces evidence is the LOCAL dev-surface probe**
   (`ui/E92/axe-windows.log`) — the Actions battery's axe suite scans pages (closed overlays);
   the open-surface scan rides this probe (the same axe stack, isolated contexts). The Actions
   keyboard walkthroughs + the sheet-z spec re-proved the surfaces' traps on the prod build.
5. **The lantern medians wander run-to-run** (3300–3522ms EN across runs 25–31) — the docs
   re-sync per dispatch by design; the D-checks fired on each regeneration and every firing
   was correct (the antibody works). This report quotes run 31, the exit dispatch.

## Reproduction commands

```bash
# the round's own artifacts
bun evidence/tools/generate-matrix.ts            # MATRIX + POINTERS + the path self-check
bun scripts/verify-docs.ts --probe               # A1–C1 · D1/D2/D3 (the J-1 antibody) · PROBE-1/2
bun scripts/verify-gatebook.ts --probe           # the ceilings drift guard (the amended CSS row)
bun evidence/tools/axe-windows.ts                # the open-window axe probe (E92)
bun scripts/audit-idioms.ts && bun scripts/audit-motion.ts && bun scripts/audit-twins.ts

# the exit battery (the sanctioned surface — GitHub Actions only)
gh workflow run production-evidence.yml          # → the full battery + the CSS-cell receipt
cat evidence/prod-run/css-cells.txt              # the measured Δ vs ui/css-baseline.txt
cat evidence/prod-run/lighthouse/medians.md      # the lantern verdict + the RECORDED devtools block

# the clock, deterministically
open "/ar?night=dusk"                            # dusk 18–20 · full 20–23 · late 23–18
```

## LANDED addendum (this round's commits)

| Item | Commit |
|---|---|
| E89 ACK (before all close-out code) | 675df08 |
| E90 the ceiling decision form (the signed amendment's record) | 476e550 |
| R9-pre: the CSS-cell instrument + the gatebook/guard amendment registration | c5aa1ab |
| The instrument's Turbopack path fix (run-29's lesson) | 8a1816e |
| The baseline receipt (Actions run 30, raw-only — zero product code) | f695251 (bot) |
| **THE REGISTER LANDS (R1–R8 · E91–E98)** | **ae2dc9c** |
| The exit battery's raws (Actions run 31, on the landed register) | (run 31's bot commit) |
| R9: this DONE + the E99 row + MATRIX/POINTERS regenerated + the r1 docs re-synced to run-31 | (this commit) |

— **STOP.** The seated roster is landed, every honestly-passable gate has passed, the ceiling
law held (+199B of 2,662B headroom), and E79's disposition stands exactly as the raws state it.
Release 2's contract (THE HONEST DOOR) is authored on this round's APPROVE — it re-cites P-096
and P-098's composition with P-046/P-056.
