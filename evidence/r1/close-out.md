# CLOSE-OUT REPORT — PROMPT 5 · RELEASE-1 CLOSE-OUT · THE LANTERN AND THE LEDGER (E88)

**Repo:** github.com/ashamsaldin303-ctrl/mirador-site · **Branch:** main
**Exit re-dispatch:** Actions run 28 (commit 25666a6's parent `915161a` + this round's raws) — the full battery re-run with the R1 repairs and the E86 dual-instrument block re-proven inside it.
**Contract:** the prompt-5 close-out contract v1.0 · ACs E82–E88 · ACK on record: `evidence/r1/E82-ACK.md` (commit 675df08, before all close-out code). All NEVERs N1–N26 binding verbatim; **N27 new and honored.**

## §8 verdict (the STOP rule, scoped — quoted)

> DONE = E82–E88 all PASS with regenerated raw evidence + the exit re-dispatch green on every component the raw files can honestly green (a FROZEN-threshold FAIL is reported as FAIL with diagnosis, never polished — N16/N21/N23/N24) + **E79's disposition on record exactly one way** + the ACK on record. When every gate that can honestly pass has passed and the disposition stands — **STOP**.

**E82–E88: PASS (raw evidence committed). E79's disposition, stated exactly one way: `carried — Option A standing`** — the honest FAIL (three failing cells, exit re-dispatch run 28: /en LCP=3361ms · /ar LCP=3911ms · /ar performance=86 score:false), the floor proof (E84), the close evaluation that priced every honest move (E85), and the UNSIGNED human decision form (E87) travel together into v2.0-I's backlog. When the disposition stands and every honestly-passable gate has passed — **STOP.**

## Per-AC table (E82–E88)

| AC | Verdict | Evidence (raw path) |
|---|---|---|
| E79 (carried from Release 1 — disposition below) | **FAIL — `carried — Option A standing`** | run 28 raw: `/en LCP=3361ms (lcp:false)` · `/ar LCP=3911ms (lcp:false)` · `/ar performance=86 (score:false)` — `prod-run/lighthouse/medians.md` + the receipts below |
| E82 ACK before the first close-out commit | PASS | `r1/E82-ACK.md` (commit 675df08) — six items verbatim + seven registered contradictions |
| E83 the ledger repairs (J-1..J-6) | PASS | `r1/E83/ledger-repairs.md` — the path self-check (156/156 tokens resolve), the E79 three-cell text in MATRIX+DONE, the D1/D2+PROBE-2 antibody, E43 units, the F6-1 H1 relabel (proven live in runs 26/27/28's regenerated lcp-element.txt), the 165KB parameterization, the J-3 re-pointing + the two deeper catches (E64's path, the never-committed r1/E66/E76/E78 audit raws — regenerated, committed, staged on every future dispatch) |
| E84 the lantern floor derivation | PASS | `docs/lcp-floor-derivation.md` (machine-generated by `tools/lcp-floor.ts` from the 48 committed simulate LHRs, cold-reproducible, every number citing its LHR path) — floors EN 1653ms · AR 1598ms; closing requires ≤3 post-TTFB slots; AR−EN = +562.5ms exactly; Load 0/Render 86–88% phase attribution; the run-20 control |
| E85 the remaining honest closes | PASS | `r1/E85/close-evaluation.md` — 10 candidates priced, ZERO landed (the only closing candidate trades the conversion surface's typography; every damage-free candidate fails the slot arithmetic; the fence-breachers rejected on the fences) |
| E86 the parallel instrument | PASS | `prod-run/lighthouse/medians.md` (the durable RECORDED-NOT-GATE block, verbatim banner) + `devtools-lhr-*.json` raws (runs 27 & 28: EN 1639/1672ms · AR 1640/1667ms, throttlingMethod=devtools asserted per LHR) — the J-2 class dead: the devtools numbers finally have their raws |
| E87 the human decision form | PASS | `docs/instrument-decision.md` — UNSIGNED; both options; the 2.5s threshold unchanged either way; the amendment log side-by-side with REPLAY; the recommendation attached as the contract-side record (provenance noted); the ACK + the DONE both state the standing default |
| **E88 the exit re-dispatch + this report** | **PASS** | Actions run 28: every component green in the raw files — 16/16 budgets · axe 0/0 · honest 404 ×2 · race guard · 0 spinners · six organs · ΣΔ ledger · cold-start · snapshots 16/16 + COP-2 2/2 + يرجى=0 · seed counts · all specs — the ONE failing step is verify-battery itself, on the E27 Lighthouse FAIL lines, exactly as the raw files say (the job's `failure` conclusion IS the honest verdict; verify-battery's verdict matches the raw files exactly). The E33 evidence commit landed (25666a6); this report + the regenerated MATRIX/POINTERS/DONE ride the close-out commit |

## E79's disposition — `carried — Option A standing`

The one-sentence form, then the receipts:

- **The honest FAIL:** three failing cells on the exit re-dispatch (run 28): `/en LCP=3361ms (lcp:false)` · `/ar LCP=3911ms (lcp:false)` · `/ar performance=86 (score:false — the LCP weight drives the AR score below the ≥90 gate; EN 91 passes)`. CLS 0.0195/0.0000 and TBT 87/94ms pass. The verdict's STRUCTURE is stable across runs 25–28 (3340/3904/87 · 3349/3888/87 · 3522/4056/85 · 3361/3911/86) — the medians wander inside the instrument's run-to-run band; the three cells never change.
- **The floor proof (why this is structural):** `docs/lcp-floor-derivation.md` — the lantern's serialized font queue (562.5ms/request, in every LHR's configSettings) floors this chain at ~1653/1598ms but prices every render-blocking face beyond the H1's at one slot; closing ≤2500ms requires deferring 3 non-LCP faces (EN) / 4 (AR) — the conversion surface itself. observedLCP == observedFCP on 48/48 committed LHRs (127–178ms); Lighthouse's own phase table reads Load Delay 0 · Load Time 0 · Render Delay 86–88%.
- **The cross-check:** the devtools-method battery, recorded beside on the same runner/build (runs 27/28): **LCP 1639/1672ms (EN) · 1640/1667ms (AR), performance 98/98** — within ~1.5% of the lantern floor model's own floors. The measured page delivers the floor's promise; the simulated queue carries the remainder. RECORDED, NOT THE GATE (N27).
- **The quantified trade:** `r1/E85/close-evaluation.md` — the only ≤2500ms-projecting candidate (C2, 2216ms both locales) defers the logo/nav/locale-switcher/body/CTA to substitute type on first paint; refused on the conversion-typography fence; every damage-free candidate fails the slot arithmetic by construction.
- **The decision form:** `docs/instrument-decision.md` — Option A stands (unsigned); the human's amendment is the only path to Option B (same runner, same 2.5s threshold).

## Known gaps (N24 — mandatory)

1. **E79 carries (three failing cells)** — diagnosed, floor-proven, enumerated, and routed to the human decision. Not polished, not relabeled, not re-surfaced (N16/N21/N23/N24).
2. **judgment-r1.md / enhancement-plan.md were never delivered as files** (ACK 6.1/6.4-class) — the decision form carries the contract-side record of the judge's recommendation with the provenance note; if the judgment file is later delivered, the form gains the true §4 quote as an amendment.
3. **Run-26's partial devtools state** (superseded by runs 27/28): its six devtools traces landed with lantern-style `localhost_*.trace.json` names (the step died before the rename); the canonical labeled raws (`devtools-*`) are runs 27/28's. The mislabeled six remain committed as raws (never deleted) with this note as their annotation.
4. **The lantern medians wander run-to-run** (3340–3522ms EN) — the docs re-sync per dispatch by design (the D-checks fire on drift; they fired twice this round, both times correctly). The final state quotes run 28, the exit re-dispatch.

## Reproduction commands

```bash
# the close-out's own artifacts
bun evidence/tools/lcp-floor.ts                  # E84 — the floor derivation, cold
bun evidence/tools/generate-matrix.ts            # E80/E83 — MATRIX + POINTERS + the path self-check (156/156)
bun scripts/verify-docs.ts --probe               # A1–C1 · D1/D2 (the J-1 antibody) · PROBE-1/2
bun scripts/audit-idioms.ts && bun scripts/audit-motion.ts && bun scripts/audit-twins.ts
bun scripts/ledger.ts && bun scripts/exchange-ledger.ts && bun scripts/verify-gatebook.ts

# the exit battery (the sanctioned surface — GitHub Actions only)
gh workflow run production-evidence.yml          # → lantern ×3 ×2 + devtools ×3 ×2 (E86 dual-block) + the full organ battery
cat evidence/prod-run/lighthouse/medians.md      # the lantern verdict + the RECORDED-NOT-GATE devtools block
bash evidence/tools/verify-battery.sh            # the verdict scan (fails on any FAIL — the honest exit gate)

# the decision record
cat docs/instrument-decision.md                  # E87 — UNSIGNED; Option A standing
```

## LANDED addendum (this round's commits)

| Item | Commit |
|---|---|
| E82 ACK (before all close-out code) | 675df08 |
| R1: the ledger repairs (J-1..J-6) + the path self-check + the E79 antibody | de0285a |
| R2: the lantern floor derivation (lcp-floor.ts + the doc) | c95ddbe |
| R3: the close evaluation (10 candidates, zero landed) | a3b1bdd |
| R4 wiring: the devtools config + the dual-instrument workflow + the dual-block summary | 48b4647 |
| run-26 lesson: `lhci collect` (autorun's default assertions) + the run-26 doc re-sync | bbc2e4b |
| Evidence batteries: runs 26/27/28 (one commit per Actions run) | 67dfba9 · 3d1ebe9 · 25666a6 |
| E86 landed (the dual-block + raws + the run-27 re-sync) | e77ea15 |
| R5: the human decision form, unsigned (E87) | 915161a |
| R6: this close-out + the final re-sync + the regenerated MATRIX/POINTERS | (this commit) |

— **STOP.** Every gate that can honestly pass has passed; the disposition stands; the human's
signature is the next move on the board. Release 2's contract is authored only on this round's
APPROVE.
