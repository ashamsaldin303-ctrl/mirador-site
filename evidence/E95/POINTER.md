# E95

- check: THE OBSERVATION DECK — the gallery hangs at staggered altitudes
- expected: gallery screenshots ×2 locales ×375+1440 · DOM order = visual order (CSS offsets only) · the sighting copy registered · the stagger utilities itemized
- actual: column offsets derived at render from index-within-collection (the mt-0/mt-8/mt-16 cycle — deterministic, SSR-stable, NO schema change per the §3 ruling); each frame a window (the existing border frame + hover:border-amber); the captions gain the authored bilingual sighting locution (8 EN + 8 AR lines, floors pinned to the six-floor spine — Floor 2/4/5/6, landed in content/{en,ar}.json, registered in copy-parity.md — 167 rows regenerated); the lightbox enters via R2's window; DOM order = visual order (CSS margins only, zero order tricks — the tab walk is the DOM walk); the stagger utilities itemized: existing spacing classes, ≈0B new CSS
- verdict: PASS
- evidence: /evidence/ui/E95/gallery--observation-deck--en--375.png + ui/E95/gallery--observation-deck--ar--1440.png

(machine-generated from /evidence/MATRIX.md — 2026-09-24T00:14:41.661Z)
