# E92

- check: THE WINDOW SYSTEM — one overlay geometry, three surfaces migrated, the dead DialogContent deleted
- expected: the three live surfaces render through the primitive · grep: ad-hoc entrance families ONLY inside the primitive + the sheet's registered exception · grep: DialogContent gone · keyboard walkthrough ×3 surfaces · before/after ×3 surfaces ×2 locales ×375 · axe 0/0 per migrated surface · the CSS+JS cells measured with P-094's net stated
- actual: src/components/ui/at-the-window.tsx (Radix Dialog underneath — focus trap · Esc · aria-modal · Title+Description): the graded night scrim (.window-scrim) + the horizon hairline at --horizon + ONE close affordance at end-4 (size-11, localized sr-only) + the settle descent / 100ms fade; dish-overlay's zoom DIED · lightbox's bespoke fades DIED · the sheet KEEPS its slide as the REGISTERED exception (idiom-contracts.md) but inherits the scrim grading + the close law (closeLabel prop); ui/dialog.tsx DELETED entirely (zero importers post-migration — DialogContent was already dead; the whole file's harvest rides the ledger); grep receipts: data-[state=open]:animate-* only in at-the-window.tsx + sheet.tsx · DialogContent = 0 hits; keyboard-walkthrough.log (open → tab trail → arrows → Esc ×3 surfaces); 12 before/after migration pairs committed (before captured at the pre-round stash, after at the migrated tree); audits green on the migrated tree (motion/twins/idioms/tokens/copy)
- verdict: PASS
- evidence: /evidence/ui/E92/keyboard-walkthrough.log + ui/E92/before--dish-overlay--en--375.png + ui/E92/after--dish-overlay--en--375.png

(machine-generated from /evidence/MATRIX.md — 2026-09-24T00:14:41.660Z)
