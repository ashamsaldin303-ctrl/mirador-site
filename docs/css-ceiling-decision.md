# THE CSS CEILING DECISION — the amendment record (prompt-6 R0.5 · E90)

**Status: the decision is SIGNED — Option B governs this round.** This form is the
in-repo RECORD of a decision the human made pre-dispatch; it is authored by the
builder and **never signed by the builder** (N29 — the human signature alone may
amend a GATEBOOK ceiling; this is the one lawful amendment path's record, the
P-092 soft-cap precedent's shape).

**The signature field, filled verbatim per the contract (§4 R0.5):**

> signed by the human, 2026-09-23 — verbatim "Option B", via PROMPT 6 v1.2's
> header; recorded by the builder, never signed by the builder

Record date: 2026-09-23 (this record authored at the round's open).

## The decision space

The prompt-6 register (seven seated UI components) carries ≈+2.55KB gross compiled
CSS against a standing +3KB additions ceiling already ≈2.97KB subscribed — ≈0.03KB
slack. The measured deletion inventory (the judge's audit, re-measured at R9's
baseline): the zoom utilities everywhere (≈143B) · the lightbox's bespoke fades
(≈145B plausible, ≈0 net after the window's scrim re-adds an equivalent fade) ·
the dead `DialogContent` + centering utilities (≈250–400B — the only directed
harvest) · TSX class-string deletions land in the JS cells, NOT the CSS cell.
**Total certain-to-plausible CSS deletion ≈0.15–0.7KB — the full register was
physically impossible under the standing ceiling.** The decision existed because
of the physics.

## Option A — the +3KB ceiling stands (NOT TAKEN)

- **Arithmetic (Appendix B's MEASURED column):** ≈0.03KB slack + ≈0.15–0.7KB
  deletable vs ≈+2.55KB gross — only P-094 (≈+0.3KB, paid by the deletion
  inventory) fits with certainty; P-101 (+0.1KB) and P-098 (+0.15KB) land only if
  the measured deletion surplus pays them; P-095/P-097/P-099/P-100 (+0.4/+0.8/
  +0.5/+0.3KB) defer with named seats (the R3-A trains — P-038/P-048/P-014's own
  diffs re-cite them).
- **The trade in product terms:** maximal discipline; a thinner signature round —
  the city keeps one entrance geometry and the menu keeps its plain headers; the
  clock, the deck, and the plates wait for their own contracts.
- **The precedent:** P-092 (the first-load soft cap 150→165KB by the human's
  signature) — the same shape: a human-signed amendment when the physics and the
  product disagree.

## Option B — the human amends the CSS ceiling to +5.5KB total (TAKEN — SIGNED)

- **The signature:** the human's message to the judge, verbatim: **"Option B"**
  (2026-09-23), quoted in PROMPT 6 v1.2's header — the signature on this decision.
- **Arithmetic (Appendix B's MEASURED column):** standing subscription ≈2.97KB +
  register gross ≈+2.55KB ≈ **5.52KB modeled** against the amended +5.5KB ceiling;
  the deletions (≈0.15–0.7KB) land as surplus margin; **all seven seated
  components land this round**; the kill order (Appendix B: P-101 → P-099's
  stagger tail → P-100 → P-097 reduced to two buckets → STOP; P-094/P-095/P-098
  never slide) is the **backstop only** — it fires solely on measured overshoot
  at R9's instrument, and its casualties would be documented as killed.
- **The trade in product terms:** the full ratified register lands — the site's
  seven signature floors in one round, a one-time +2.5KB CSS cost against a
  ≈291KB JS wire budget (the CSS cell's own law, ≤5.5KB cumulative from this
  round's start).
- **The precedent:** P-092's shape — the amendment belongs to the human
  signature alone, logged here. **Agents never touch this cell again (N29).**

## The governed state

- The round starts and ends under **Option B**: CSS ceiling **+5.5KB total**, all
  seven components landing, kill order backstop-only.
- `docs/gatebook.md`'s CSS row registers the amendment in the P-092 arrow form
  ("+3KB → **+5.5KB** total — human-signed amendment 2026-09-23, Option B"),
  with `scripts/verify-gatebook.ts`'s expectation updated in the same act — a
  coordinated record-keeping change, never an agent amendment.
- The Exchange Ledger's cell ⑤ (R9's new CSS-cell instrument) measures the
  compiled route CSS (gz) on the Actions build against the pre-round baseline
  receipt, files the Δ, and asserts the cumulative ≤5.5KB law.
- **No further input is needed on this decision** (§9(2)). The judge's
  recommendation on record (B — consistent with the human's stated
  distinctiveness priority) is moot beside the signature itself.
